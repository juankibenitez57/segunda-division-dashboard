services:
  - type: web
    name: betis-scoutgpt-proxy
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 scoutgpt_proxy.py
    healthCheckPath: /status
